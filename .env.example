# Upstash Redis (already configured in Vercel)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Anthropic (already configured in Vercel)
ANTHROPIC_API_KEY=

# Cloudinary - add these to Vercel environment variables
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=your_cloud_name_here
NEXT_PUBLIC_CLOUDINARY_UPLOAD_PRESET=your_upload_preset_here
